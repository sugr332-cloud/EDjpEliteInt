# Japanese: not yet translated (out of P0 scope - see docs/ELITEINTEL_INTEGRATION_PLAN.md Phase 2/3).
# Every key here falls back to English via AiActionAliasTextProvider's fallback bundle - meaning
# Japanese voice commands are not yet recognized by phrase (moot for now: ParakeetSTTImpl cannot
# transcribe Japanese speech at all yet either, see its toLangCode() comment). This file exists
# (rather than being absent) only because BundleKeyParityTest/BundleQuotingTest require every declared
# Language to have a bundle file on the classpath; the resulting key gaps are tracked in
# src/test/resources/i18n-parity-baseline.txt, not hidden.
